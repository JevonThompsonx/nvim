local options = {
  ensure_installed = { "lua", 'html', 'css', 'python', 'javascript', 'typescript', 'vue'},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
