local options = {
  ensure_installed = { "rust", "python", "javascript", "typescript", "css", "html", "vue", "tsx" },
  auto_install = true,

  highlight = {
    enable = true,
  },
  indent = {
    enable = true,
  },
}

return options
